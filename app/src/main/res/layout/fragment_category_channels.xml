<?xml version="1.0" encoding="utf-8"?>
<androidx.swiperefreshlayout.widget.SwipeRefreshLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/swipe_refresh"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Groups Header with TabLayout (Only visible when groups exist) -->
        <LinearLayout
            android:id="@+id/groups_header"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:background="?attr/colorSurface"
            android:visibility="gone"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <!-- Group Icon Button (Menu icon) -->
            <ImageView
                android:id="@+id/groups_icon"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginStart="4dp"
                android:padding="12dp"
                android:src="@drawable/ic_category"
                android:contentDescription="Groups"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:focusable="true"
                android:focusableInTouchMode="false"
                app:tint="?android:attr/textColorSecondary" />

            <!-- TabLayout for Groups -->
            <com.google.android.material.tabs.TabLayout
                android:id="@+id/tab_layout_groups"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_weight="1"
                android:background="@android:color/transparent"
                app:tabMode="scrollable"
                app:tabGravity="start"
                app:tabIndicatorColor="#EF4444"
                app:tabIndicatorHeight="3dp"
                app:tabSelectedTextColor="#EF4444"
                app:tabTextColor="#888888"
                app:tabTextAppearance="@style/TabTextAppearance"
                app:tabRippleColor="#40EF4444"
                app:tabIndicatorFullWidth="false"
                app:tabPaddingStart="16dp"
                app:tabPaddingEnd="16dp"
                app:tabMinWidth="60dp" />
        </LinearLayout>

        <!-- Divider (Only visible when groups exist) -->
        <View
            android:id="@+id/header_divider"
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:background="?android:attr/dividerHorizontal"
            android:alpha="0.2"
            android:visibility="gone"
            app:layout_constraintTop_toBottomOf="@id/groups_header"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler_view_channels"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:padding="4dp"
            android:clipToPadding="false"
            app:layout_constraintTop_toBottomOf="@id/header_divider"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <ProgressBar
            android:id="@+id/progress_bar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="gone"
            app:layout_constraintTop_toTopOf="@id/recycler_view_channels"
            app:layout_constraintBottom_toBottomOf="@id/recycler_view_channels"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <TextView
            android:id="@+id/empty_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="No channels available"
            android:visibility="gone"
            android:textSize="16sp"
            android:textColor="?android:attr/textColorSecondary"
            android:fontFamily="@font/bergen_sans"
            app:layout_constraintTop_toTopOf="@id/recycler_view_channels"
            app:layout_constraintBottom_toBottomOf="@id/recycler_view_channels"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.swiperefreshlayout.widget.SwipeRefreshLayout>
